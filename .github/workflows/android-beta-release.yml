name: Android beta release

on:
  push:
    tags:
      - "v*-beta.*"

permissions:
  contents: write

concurrency:
  group: android-beta-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build-test-and-release:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - name: Check out the tagged source
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          fetch-depth: 0

      - name: Require the tagged commit to be on main
        shell: bash
        run: |
          set -euo pipefail
          git fetch origin main --no-tags
          if ! git merge-base --is-ancestor "$GITHUB_SHA" origin/main; then
            echo "::error::Android beta tags must point to a commit already present on main."
            exit 1
          fi

      - name: Check whether trusted signing is configured
        id: signing_key
        shell: bash
        env:
          KEYSTORE_BASE64: ${{ secrets.DICTA_BETA_KEYSTORE_BASE64 }}
        run: |
          if [[ -z "${KEYSTORE_BASE64:-}" ]]; then
            echo "::warning::Missing DICTA_BETA_KEYSTORE_BASE64. The optional release workflow is skipped; publish only after the local signing check succeeds."
            echo "available=false" >> "$GITHUB_OUTPUT"
          else
            echo "available=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Set up JDK 17
        if: steps.signing_key.outputs.available == 'true'
        uses: actions/setup-java@b6effb05e454b25005698d916606bdc6ffcbf961 # v5
        with:
          distribution: temurin
          java-version: "17"
          cache: gradle

      - name: Install the pinned Android SDK components
        if: steps.signing_key.outputs.available == 'true'
        shell: bash
        run: |
          set -euo pipefail
          "${ANDROID_HOME}/cmdline-tools/latest/bin/sdkmanager" "platforms;android-35" "build-tools;35.0.0"

      - name: Verify tag and Gradle versions
        if: steps.signing_key.outputs.available == 'true'
        shell: pwsh
        run: ./android/scripts/verify-beta-version.ps1 -Tag "${{ github.ref_name }}" -CheckGitHistory

      - name: Reconstruct the exact beta keystore
        if: steps.signing_key.outputs.available == 'true'
        shell: bash
        env:
          KEYSTORE_BASE64: ${{ secrets.DICTA_BETA_KEYSTORE_BASE64 }}
        run: |
          set -euo pipefail
          printf '%s' "$KEYSTORE_BASE64" | base64 --decode > android/beta-debug.keystore
          chmod 600 android/beta-debug.keystore
          expected="$(tr -d '[:space:]' < android/keystore-file-sha256.txt)"
          actual="$(sha256sum android/beta-debug.keystore | cut -d ' ' -f 1 | tr '[:lower:]' '[:upper:]')"
          if [[ "$actual" != "$expected" ]]; then
            echo "::error::The reconstructed keystore is not the trusted beta keystore."
            exit 1
          fi

      - name: Test and build the signed beta APK
        if: steps.signing_key.outputs.available == 'true'
        working-directory: android
        shell: bash
        run: |
          set -euo pipefail
          chmod +x gradlew
          ./gradlew --no-daemon testBetaReleaseUnitTest lintBetaRelease assembleBetaRelease

      - name: Verify the APK signing certificate
        if: steps.signing_key.outputs.available == 'true'
        shell: pwsh
        run: ./android/scripts/verify-android-signing.ps1

      - name: Prepare the release asset
        if: steps.signing_key.outputs.available == 'true'
        shell: bash
        run: cp android/app/build/outputs/apk/beta/release/app-beta-release.apk "$RUNNER_TEMP/copy-challenge-beta.apk"

      - name: Publish the GitHub prerelease
        if: steps.signing_key.outputs.available == 'true'
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          gh release create "${GITHUB_REF_NAME}" \
            "$RUNNER_TEMP/copy-challenge-beta.apk#copy-challenge-beta.apk" \
            --repo "$GITHUB_REPOSITORY" \
            --verify-tag \
            --prerelease \
            --title "Copy Challenge Android ${GITHUB_REF_NAME#v}" \
            --generate-notes
